{
  "name": "<%= _.slugify(projectName) %>",
  "version": "0.0.1",
  "dependencies": {
    "normalize-css": "latest",
    <% if (includeBourbon) { %>"bourbon": "latest",<% } %>
    "jquery": "latest"
  }
}
